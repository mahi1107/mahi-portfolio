# Base URL for the dynamic portfolio backend API server.
# During local development, this defaults to http://localhost:5000.
# For production, set this to your deployed backend service URL (e.g. Render, Railway, or Heroku).
# If left blank, the app will gracefully fall back to local static definitions.
VITE_API_BASE_URL=http://localhost:5000
